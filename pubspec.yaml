name: nora
description: NORA - Community app
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
    
  # Tes packages
  image_picker: ^1.2.1
  supabase_flutter: ^2.8.0
  lottie: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # CORRECTION : On liste chaque fichier un par un pour forcer l'inclusion
  assets:
    - assets/animations/community.json
    - assets/animations/hello.json
    - assets/animations/login.json
    - assets/animations/paperplane.json